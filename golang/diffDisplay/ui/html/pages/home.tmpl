{{define "title"}}Home{{end}}

{{define "main"}}
    <h2>start of table...</h2>
    <button class="base-button accept-button"
            hx-get="/clicked"
            hx-target="#diff-results"
            hx-confirm="Are you sure">Accept</button>
    <button class="base-button cancel-button"
            hx-get="/clicked"
            hx-target="#diff-results"
            hx-confirm="Are you sure">Cancel</button>
    <h2>h2 text</h2>
    <p>some text</p>
    <div id="diff-results">
        <p>waiting for server...</p>
    </div>
{{end}}

